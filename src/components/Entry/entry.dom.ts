const domPrefix = "entry-component";
export const entryValueDomSelector = `js-${domPrefix}-entry-value`;
export const entryOptionsSelector = `js-${domPrefix}-options-toggle`;
export const entryEditMenuItemSelector = `js-${domPrefix}-edit-menu-tem`;
