import { CLIENT_ONLY_PATH_PREFIX } from "./client-only-prefix";

export const SYNC_PREVIEW_ROUTE = CLIENT_ONLY_PATH_PREFIX + "/sync/preview";
export const SYNC_PREVIEW_RESULT = CLIENT_ONLY_PATH_PREFIX + "/sync/result";
